{
  "name": "provable-think-e2e-test-worker",
  "version": "0.0.1",
  "private": true,
  "description": "End-to-end quality gate for the provable-think package. Imports the package as a file: dependency, wraps a stub Think class with withProvenance, and exposes endpoints to fund + trigger commits + verify mainnet anchoring.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@bsv/sdk": "2.0.13",
    "provable-think": "file:.."
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260128.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.95.0"
  }
}
